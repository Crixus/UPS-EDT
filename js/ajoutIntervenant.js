
var verification_tel = function(select) {/*
	// var value = select.value;
	var value = document.getElementsByName('telephone')[0].value;
	if (isNaN(value)) {
		// var nombre = value.replace('/[0-9]+[.,]?([0-9]+)?\/g','$&');
		var nombre = intval(value);
		// select.value = nombre;
		document.getElementsByName('telephone')[0].value = nombre;
	}*/
}